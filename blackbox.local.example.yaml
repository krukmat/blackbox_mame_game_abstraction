# Optional local bootstrap settings for `apps/mame-harness/cli.py doctor`.
# Environment variables from `.env` or your shell take precedence.
# Do not commit a populated `blackbox.local.yaml`.

source_profile: gng
mame_driver: gngb
mame_binary: mame
ffmpeg_binary: ffmpeg
rom_path: REPLACE_ME_PRIVATE_ROM_DIR
evidence_root: evidence/private
boot_plan: plans/gng_boot_only.yaml
trace_input_plan: plans/gng_gameplay.yaml
trace_output: specs/traces/gng_trace.json
