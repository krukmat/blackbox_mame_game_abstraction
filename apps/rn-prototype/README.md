# React Native Prototype

This directory contains a deterministic TypeScript gameplay-core scaffold for the independent mobile implementation.

- Input surface: generated mechanics and entity specs only.
- Rendering surface: placeholder geometry and abstract renderer adapters only.
- Guardrail: no original sprites, screenshots, audio, or frame-derived crops.

Local test command:

```bash
npm install
npm test
```
