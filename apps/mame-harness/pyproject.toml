[project]
name = "mame-harness"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "Pillow>=12.0",
    "PyYAML>=6.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
