__pycache__/
.pytest_cache/
.venv/
.env
.env.local
blackbox.local.yaml
# T20.5 / ADR-026 — local RAM address map (real addresses; never commit). Only the
# blackbox.local.memory_map.example.yaml template is tracked.
blackbox.local.memory_map.yaml
# T30.3 / ADR-028 — local integration bundle (real addresses; never commit). Only the
# blackbox.local.integration_bundle.example.yaml template is tracked.
blackbox.local.integration_bundle.yaml
*.pyc
node_modules/

# Private clean-room evidence
evidence/private/**
!evidence/private/.gitkeep

# ROMs and emulator artifacts
*.zip
*.7z
*.rom
*.bin
*.nes
*.sfc
*.smc
*.gba
*.gb
*.gbc
*.gen
*.cue
*.iso
*.chd
*.state
*.sta
*.nv
*.sav

# Raw captures and video
captures/**
frames/**
crops/**
*.mp4
*.avi
*.mov
*.mkv
*.webm
*.wav
*.flac

# Generated private datasets
*.npz
*.npy
*.parquet
snap/gngb/*
evidence/.DS_Store
