# T20.11 — End-to-End Clean-Room + Reproducibility Verification

## Objective
Audit the full re-architected pipeline for determinism and clean-room safety, and finalize all
documentation (ADRs, indices, Obsidian vault, Known Gaps).

## Scope
- Determinism check: same experiment plans → identical calibration values across two runs.
- Clean-room sweep: no public artifact contains private paths, addresses, ROM-derived strings,
  pixel content, or copyright proper nouns (extend guardrail tests as needed).
- Verify the full import graph (per the existing T11.5-style audit) after all changes land.
- Confirm ADR Index consistency across `CLAUDE.md`, `AGENTS.md`, `docs/obsidian/README.md`,
  and `docs/obsidian/00 - Project Overview.md`; resolve/close superseded Known Gaps
  (ADR-013 scroll, ADR-012/021 signatures, ADR-019 default).
- Run the full test suite on the harness venv; record the green baseline count.

## Out of scope
- New capabilities.

## Dependencies
All prior T20.x.

## Reasoning grade
Medium — comprehensive but mechanical verification.

## Effort grade
Medium.

## Acceptance criteria
- Calibration reproducibility demonstrated (identical values across runs).
- Guardrail/clean-room sweep passes with extended coverage for the new artifacts.
- Import-graph audit clean.
- All ADR indices consistent; superseded Known Gaps closed/updated.
- Full test suite green on `apps/mame-harness/.venv`; baseline count recorded.

## Recommended model
Sonnet.

## Reference documents
- `docs/plans/automated_mapping_pipeline_plan.md`
- ADR-001, ADR-003, ADR-006, ADR-008, and ADR-023..027
- `apps/mame-harness/guardrails.py`, existing T11.5 verification approach
- `CLAUDE.md`, `AGENTS.md`, `docs/obsidian/`

## Agent handoff prompt
"Audit the re-architected pipeline: prove calibration reproducibility, run an extended
clean-room guardrail sweep over all new artifacts, verify the import graph, reconcile every ADR
index, close superseded Known Gaps, and confirm the full test suite is green on the harness
venv. Record the baseline test count."
