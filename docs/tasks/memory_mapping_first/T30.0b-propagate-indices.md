# T30.0b — Propagate ADR-028 into the four ADR indices

## Objective

Add ADR-028 to the four canonical ADR indices and add one fallback-demotion note. Every string
to insert is given verbatim below. No synthesis. Use the Edit tool with the literal strings.

Run **after** T30.0a (the ADR file and Obsidian note must already exist).

## Reasoning grade

Low — exact insertions at named anchors.

## Effort grade

Low — six edits across four files.

## Recommended model

Sonnet (`claude-sonnet-4-6`) — thinking **Off**.

## Edits

### File 1 — `CLAUDE.md`

1a. In the `### ADR Index` table, append this as the **new last row** (immediately after the
existing `| ADR-026 | ... memory-tap calibration (T20.5/T20.6) |` row):

```
| ADR-028 | Memory-mapping-first game integration: the MAME Lua memory tap (ADR-026) becomes the **default** entity position/state source; CV (ADR-012/013/021/022) and pickers (ADR-019/020) demoted to fallback; declarative per-game integration bundle (typed variables + scenario events + rom.sha + savestate anchor) + guided address-search; calibration via ADR-024 over the RAM state timeline; deprecates unauthored ADR-025/027 and tasks T20.7–T20.10 | memory tap + bundle (T30.2–T30.5), `apps/mame-harness/guardrails.py`, `docs/plans/memory_mapping_first_rearchitecture_plan.md` |
```

1b. Immediately **after** that ADR Index table (after the new ADR-028 row, before the
`### Known Gaps` heading), insert this blockquote on its own line:

```
> **ADR-028 (2026-06-18):** ADR-012, ADR-013, ADR-019, ADR-020, ADR-021, ADR-022 are demoted to *fallback* by ADR-028 (memory-mapping-first). The memory tap (ADR-026) is the default position/state source.
```

### File 2 — `AGENTS.md`

2a. Same as 1a: append the **same ADR-028 row** as the new last row of the `### ADR Index`
table (immediately after the `| ADR-026 | ... |` row, around line 120).

2b. Same as 1b: insert the **same blockquote note** immediately after that table.

### File 3 — `docs/obsidian/README.md`

3a. In the ADR list, immediately after the line
`- [[ADR-026 Internal-State Observation Boundary]] (Accepted — constrained)`, add:

```
- [[ADR-028 Memory-Mapping-First Integration]] (Proposed — memory tap default; CV fallback)
```

### File 4 — `docs/obsidian/00 - Project Overview.md`

4a. In the `## ADR Index` table, append this as the **new last row** (immediately after the
existing `| [[ADR-026 Internal-State Observation Boundary]] | ... |` row):

```
| [[ADR-028 Memory-Mapping-First Integration]] | Memory tap becomes the default position/state source; CV demoted to fallback; declarative integration bundle + guided address search; deprecates the unauthored vision rewrite |
```

## Acceptance criteria

- `CLAUDE.md` and `AGENTS.md` each contain the ADR-028 row and the fallback blockquote.
- `docs/obsidian/README.md` contains the ADR-028 list line.
- `docs/obsidian/00 - Project Overview.md` contains the ADR-028 table row.
- No row was deleted; no other content changed; tables stay well-formed (3 columns in
  CLAUDE.md/AGENTS.md, 2 columns in the Overview).

## Stop condition

Stop after the four files are updated. Do not start T30.0c.
</content>
