# T10.7 ST7 — Full Verification After Remediation

## Status

🔶 Verification run completed — follow-up required before ST7 can be marked complete.

Latest run summary (2026-05-24):
- `apps/mame-harness/.venv/bin/pytest` from `apps/mame-harness/`:
  `340 passed, 14 failed, 1 skipped`
- `apps/mame-harness/.venv/bin/pytest packages/vision/tests/` from repo root:
  `70 passed, 1 failed, 4 xfailed`
- `npm test -- --runInBand` from `apps/rn-prototype/`:
  `8 passed, 0 failed`

Failure classification:
- The 14 harness failures are the known pre-existing `tests/test_episode_extractor.py`
  relative-path issue (`specs/traces/gng_trace.json` resolved from the wrong cwd),
  already documented in `docs/handoff_t11_3_physics_calibration.md`.
- The single vision failure is a stale expectation in
  `packages/vision/tests/test_trace_extractor_multi.py`:
  the test fixture uses a 22×25 region (`ratio ≈ 0.0096`), which correctly
  classifies as `enemy_a`, while the assertion expects `player`
  (`_RATIO_PLAYER = 0.04`).

ST7 remains open because the verification layer is not green yet, even though
the only newly surfaced issue appears to be a test bug rather than a production
regression.

## Parent Task

[T10.7 — Tracker Continuity Fix](./T10.7-tracker-continuity-fix.md)

## Objective

Run the full verification pass for the completed T10.7 remediation chain and
confirm that the Python harness, vision tests, and RN prototype tests still
pass with the current calibrated artifacts and documentation state.

## Scope

- Run the harness Python test suite from `apps/mame-harness/` using the project
  virtualenv
- Run the vision test suite against `packages/vision/tests/` using the project
  virtualenv
- Run the RN prototype test suite from `apps/rn-prototype/`
- Record any failures with enough detail to classify whether they are:
  - new regressions introduced by T10.7 work
  - pre-existing failures unrelated to T10.7
  - environment/configuration issues
- Mark ST7 complete in the parent T10.7 task file only if the verification
  result is acceptable under the documented acceptance criteria

## Out of Scope

- Fixing failing tests within this task file itself
- New code changes outside minimal test-environment adjustments that are
  strictly required to execute the suites
- Additional calibration work, trace regeneration, or episode regeneration
- T11.4 RN scene implementation

## Dependencies

- ✅ T10.7 ST5 complete — mechanics and episodes synced
- ✅ T10.7 ST6 complete — documentation aligned with the post-remediation state
- ✅ Project virtualenv available at `apps/mame-harness/.venv/`

## Reasoning Grade

`Low`

Reasoning basis: this is a verification-only task with a fixed command
sequence, but it still needs disciplined triage because failures must be
classified correctly rather than treated as automatic regressions.

## Effort Grade

`Low`

## Recommended Model

`gpt-5.4`

## Acceptance Criteria

1. `apps/mame-harness/.venv/bin/pytest` runs successfully for the intended
   harness test target from `apps/mame-harness/`, or any failure is explicitly
   classified as pre-existing or unrelated to T10.7.
2. `apps/mame-harness/.venv/bin/pytest packages/vision/tests/` runs
   successfully from the repo root, or any failure is explicitly classified as
   pre-existing or unrelated to T10.7.
3. `npm test` runs successfully from `apps/rn-prototype/`, or any failure is
   explicitly classified as pre-existing or unrelated to T10.7.
4. No tests are deleted, skipped, or weakened to manufacture a pass.
5. The verification summary clearly states whether T10.7 can be considered
   complete at the suite-verification layer, or whether follow-up work is
   required.
6. `docs/tasks/gng_source_integration/T10.7-tracker-continuity-fix.md` marks
   ST7 complete only if the verification outcome satisfies the task contract.

## Execution Order

1. Harness suite: run from `apps/mame-harness/`
2. Vision suite: run from repo root against `packages/vision/tests/`
3. RN suite: run from `apps/rn-prototype/`
4. Triage and classify failures
5. Update parent task status if and only if the verification outcome supports
   completion

## Reference Documents

- [This Task File](./T10.7.ST7-full-verification.md)
- [Parent Task — T10.7](./T10.7-tracker-continuity-fix.md)
- [Parent Plan — GNG Source Integration](../../plans/gng_source_integration_plan.md)
- [README.md](../../../README.md)
- [AGENTS.md](../../../AGENTS.md)
- [CLAUDE.md](../../../CLAUDE.md)
- [ADR-006 — Vision Layer Numeric-Only Output](../../adr/ADR-006-vision-layer-numeric-only-output.md)
- [ADR-012 — Entity Signature-Based Player Identification](../../adr/ADR-012-entity-signature-based-player-identification.md)
- [ADR-013 — OpenCV Vision Backend](../../adr/ADR-013-opencv-vision-backend.md)
- [Vision Layer module note](../../obsidian/Vision%20Layer.md)
- [T11.2 task](./T11.2-trace-episode-extractor.md)
- [T11.3 task](./T11.3-physics-calibration.md)
