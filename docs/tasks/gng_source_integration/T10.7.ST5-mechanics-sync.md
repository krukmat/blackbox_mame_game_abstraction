# T10.7 ST5 — Mechanics YAML Sync + Episode Regeneration

## Status

✅ Complete — mechanics YAML synced and episodes regenerated; acceptance gate passed.

## Parent Task

[T10.7 — Tracker Continuity Fix](./T10.7-tracker-continuity-fix.md)

## Objective

Bring `specs/mechanics/gng_abstract_mechanics.yaml` into structural agreement
with `specs/calibration/gng_physics_calibration.yaml`, then regenerate
`specs/episodes/gng_episodes.json` from the current trace and verify the
T11.2 acceptance gate.

## Scope

- Sync `mechanics.jump_arc.fields.velocity_y` to the calibration value
  (`0.007825 / 0.4668`, n=2, `human_validated_kinematic`).
- Sync `mechanics.gravity_collision.fields.gravity_units_per_frame` to the
  calibration value (`0.001956 / 0.1167`, n=2, `human_validated_kinematic`).
- Remove stale `note` field on `jump_arc.velocity_y` that referenced
  PhysicsSystem sign inversion and the obsolete 1.9966 value.
- Refresh mechanics YAML header comment to match the current calibration
  provenance (T10.7.A / T10.7.C / T10.7.E).
- Run `apps/mame-harness/episode_extractor.py` and replace
  `specs/episodes/gng_episodes.json`.
- Confirm the result contains ≥3 episodes, each with ≥1 `jump_start` event
  for the player entity.

## Out of Scope

- `apps/rn-prototype/src/engine/PhysicsSystem.ts` changes
  (`CALIBRATED_PHYSICS_CONFIG` already matches the calibration YAML).
- Adding `projectileVelocity` to `PhysicsConfig` (no consumer yet; defer to
  T11.4).
- `apps/rn-prototype/tests/engine.test.ts` assertion changes (current
  assertions still pass against the synced values).
- ADR-013 / Obsidian / CLAUDE.md / AGENTS.md updates (that is ST6).
- Full test suite run (that is ST7).
- Re-running any picker tool or calibrator.

## Dependencies

- ✅ T10.7.A — jump_velocity_y and gravity_units_per_frame calibrated.
- ✅ T10.7.C — locomotion_velocity_x calibrated.
- ✅ T10.7.E — projectile_velocity_x calibrated; trace regenerated from
  `run_t10_7_proj2`.

## Reasoning Grade

`Low`

Reasoning basis: a mechanical numeric sync between two YAML files plus a
single-command regeneration of a public artifact. No new logic, no new
abstraction. The validation gate is binary against a previously documented
threshold.

## Effort Grade

`S`

## Recommended Model

`Sonnet`

## Acceptance Criteria

1. `specs/mechanics/gng_abstract_mechanics.yaml`:
   `jump_arc.fields.velocity_y.calibrated_value == 0.007825` and
   `calibrated_value_per_second == 0.4668`, with `sample_count: 2` and the
   stale `note` removed.
2. `specs/mechanics/gng_abstract_mechanics.yaml`:
   `gravity_collision.fields.gravity_units_per_frame.calibrated_value ==
   0.001956` and `calibrated_value_per_second == 0.1167`, with
   `sample_count: 2`.
3. The mechanics YAML header comment reflects the T10.7.A/C/E provenance
   (no lingering T11.3 attribution for the calibrated values).
4. `specs/episodes/gng_episodes.json` was regenerated by
   `apps/mame-harness/episode_extractor.py` from the current public trace.
5. The regenerated `gng_episodes.json` contains ≥3 episode entries.
6. Every episode in the regenerated artifact has at least one frame whose
   `player` block lists `jump_start` in its events array.
7. No public artifact references a path under `evidence/private/` after the
   regeneration (guardrails layer enforces; verification is implicit).
8. Parent task `T10.7-tracker-continuity-fix.md` ST5 row marked complete.
9. `docs/plans/gng_source_integration_plan.md` ST5 line marked complete.

## Subtasks

| ID | Title | Effort | Reasoning |
|----|-------|--------|-----------|
| ST5.1 | Sync `jump_arc.velocity_y` block in mechanics YAML | S | Low |
| ST5.2 | Sync `gravity_collision.gravity_units_per_frame` block in mechanics YAML | S | Low |
| ST5.3 | Refresh mechanics YAML header comment | S | Low |
| ST5.4 | Run `episode_extractor.py` and replace `specs/episodes/gng_episodes.json` | S | Low |
| ST5.5 | Verify T11.2 acceptance gate (≥3 episodes, ≥1 jump_start each) | S | Low |
| ST5.6 | Mark ST5 complete in parent task and integration plan | S | Low |

## Reference Documents

- [This Task](./T10.7.ST5-mechanics-sync.md)
- [Plan — T10.7.ST5-mechanics-sync.md](../../plans/T10.7.ST5-mechanics-sync.md)
- [Parent Task — T10.7-tracker-continuity-fix.md](./T10.7-tracker-continuity-fix.md)
- [Prior Subtask — T10.7.E-projectile-in-flight-tracking.md](./T10.7.E-projectile-in-flight-tracking.md)
- [T11.2 task (consumer of regenerated episodes)](./T11.2-trace-episode-extractor.md)
- [Calibration source YAML](../../../specs/calibration/gng_physics_calibration.yaml)
- [Mechanics target YAML](../../../specs/mechanics/gng_abstract_mechanics.yaml)
- [Episode extractor](../../../apps/mame-harness/episode_extractor.py)
- [Integration plan](../../plans/gng_source_integration_plan.md)
- [README.md](../../../README.md)
- [AGENTS.md](../../../AGENTS.md)
- [CLAUDE.md](../../../CLAUDE.md)

## Agent Handoff Prompt

> **T10.7 ST5 — Mechanics YAML Sync + Episode Regeneration.** Read
> `docs/plans/T10.7.ST5-mechanics-sync.md` and
> `docs/tasks/gng_source_integration/T10.7.ST5-mechanics-sync.md` in full.
> Source values come from `specs/calibration/gng_physics_calibration.yaml`
> (canonical). Target file is `specs/mechanics/gng_abstract_mechanics.yaml`.
> Use surgical Edit operations on only the `jump_arc.velocity_y` and
> `gravity_collision.gravity_units_per_frame` blocks plus the header comment;
> leave the rest of the mechanics YAML untouched. Then run
> `apps/mame-harness/.venv/bin/python apps/mame-harness/episode_extractor.py`
> (verify CLI signature first by reading the file) to regenerate
> `specs/episodes/gng_episodes.json`. Validate the T11.2 acceptance gate
> programmatically: load the JSON, assert episode count ≥3, assert every
> episode contains at least one `jump_start` event in its player frames.
> If the gate fails, surface the failure and stop — do not patch the
> threshold. Mark ST5 complete in both
> `docs/tasks/gng_source_integration/T10.7-tracker-continuity-fix.md` and
> `docs/plans/gng_source_integration_plan.md` only after the gate passes.
> Communication with the operator is in Spanish; documentation in English.
